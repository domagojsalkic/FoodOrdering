@model FoodOrder.ViewModels.AllOrdersViewModel
@{
    ViewData["Title"] = "AllOrders2";
}

<h1>All orders</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Order ID")
            </th>
            <th>
                @Html.DisplayName("Total price")
            </th>
            <th>
                @Html.DisplayName("Payment method")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Orders)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalPrice)
                </td>
                <td>
                    @foreach (var item2 in Model.Checks)
                    {
                        if (item.Id == item2.OrderId)
                            {
                                @Html.DisplayFor(modelItem=>item2.Payment.PaymentMethod)
                            }
                    }
                </td>
                <td>
                    <div class="form-group row">
                        @if (!item.IsPayed)
                        {
                            <div class="col-3">
                                <a class="btn btn-outline-warning" asp-controller="Checks" asp-action="Create" asp-route-id="@item.Id">Check out</a>

                            </div>
                            <div class="col-3">
                                <input type="submit" value="Delete" asp-route-id="@Model.Orders" class="btn btn-danger form-control" />
                            </div>
                        }
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>